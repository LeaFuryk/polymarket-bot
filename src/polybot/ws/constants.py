"""WebSocket module constants."""

# Default server bind host
DEFAULT_WS_HOST: str = "0.0.0.0"

# Default server bind port
DEFAULT_WS_PORT: int = 8765

# WebSocket ping interval in seconds (keep-alive)
PING_INTERVAL_SECONDS: int = 20

# WebSocket ping timeout in seconds
PING_TIMEOUT_SECONDS: int = 20
